# Release staging

Run `scripts\package.ps1` to build and test the ASI mod, verify Ultimate ASI Loader, and create:

```text
artifacts\DA2-Cinematic-UI-Hider-v0.1.0.zip
```

Release binaries are attached to GitHub Releases rather than committed to the repository.
